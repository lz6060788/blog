# ============================================================================
# 环境变量配置模板
# ============================================================================
# 这是一个参考模板，复制为 .env.local 或 .env.production 后填写实际值

# ============================================================================
# NextAuth.js 认证配置
# ============================================================================

# AUTH_SECRET - NextAuth.js 加密密钥
# 用途：加密 JWT token 和会话
# 生成方法：openssl rand -base64 32
# ⚠️ 重要：生产环境必须使用强密钥，不要与开发环境共用
AUTH_SECRET=your_auth_secret_here

# NEXTAUTH_URL - 应用的完整 URL
# 开发环境: http://localhost:3000
# 生产环境: https://yourdomain.com
NEXTAUTH_URL=http://localhost:3000

# ============================================================================
# OAuth 提供商配置
# ============================================================================

# GitHub OAuth
# 创建地址：https://github.com/settings/developers
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

# Google OAuth（可选）
# 创建地址：https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret

# ============================================================================
# 其他配置（未来扩展）
# ============================================================================

# 数据库路径（如果需要自定义）
# DATABASE_URL=./data/db.sqlite

# Node.js 运行时（NextAuth.js 需要）
# NEXTAUTH_RUNTIME=nodejs
