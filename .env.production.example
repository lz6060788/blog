# ============================================================================
# 生产环境配置模板
# .env.production - 生产环境使用
# ============================================================================
# 使用说明：
# 1. 复制此文件为 .env.production
# 2. 填写实际的生产环境凭据
# 3. 在部署平台（Vercel/Netlify 等）配置环境变量时使用这些值
# 4. 不要将 .env.production 提交到 Git

# NextAuth.js 配置
# ⚠️ 重要：生产环境必须使用新的、安全的 AUTH_SECRET
# 生成方法: openssl rand -base64 32
AUTH_SECRET=please_generate_new_secret_for_production

# 生产环境 URL（部署后替换为实际域名）
NEXTAUTH_URL=https://yourdomain.com

# ----------------------------------------------------------------------------
# GitHub OAuth - 生产环境配置
# ----------------------------------------------------------------------------
# 创建步骤：
# 1. 在 GitHub OAuth Apps 中添加新的应用或配置生产环境 URL
# 2. 填写生产环境信息：
#    - Application name: My Blog (Production)
#    - Homepage URL: https://yourdomain.com
#    - Authorization callback URL: https://yourdomain.com/api/auth/callback/github
# 3. 可以与开发环境使用同一个 OAuth App，或创建新的

GITHUB_CLIENT_ID=production_github_client_id
GITHUB_CLIENT_SECRET=production_github_client_secret

# ----------------------------------------------------------------------------
# Google OAuth - 生产环境配置（可选）
# ----------------------------------------------------------------------------
# 创建步骤：
# 1. 在 Google Cloud Console OAuth 2.0 Client ID 中添加生产环境 URL
# 2. 添加授权的重定向 URI: https://yourdomain.com/api/auth/callback/google
# 3. 复制生产环境的 Client ID 和 Secret

GOOGLE_CLIENT_ID=production_google_client_id
GOOGLE_CLIENT_SECRET=production_google_client_secret
