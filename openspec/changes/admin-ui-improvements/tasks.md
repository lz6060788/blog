## 1. Input 组件样式主题化

- [x] 1.1 搜索并统计项目中所有使用原生 `<input>` 和 `<textarea>` 的位置
- [x] 1.2 确认 shadcn/ui Input 和 Textarea 组件已安装并可用
- [x] 1.3 在 `app/admin/globals.css` 或组件样式文件中定义 Input 组件的主题化样式
- [x] 1.4 替换登录页的原生 input 为 Input 组件，并验证样式（登录页使用 OAuth，无需修改）
- [x] 1.5 替换文章列表页的搜索框（如果有）为 Input 组件
- [x] 1.6 替换文章编辑页中的原生 input/textarea 为 Input/Textarea 组件（分类 select 将在任务 5.5 中替换为 Select 组件）
- [x] 1.7 替换分类管理和标签管理页面的原生 input 为 Input 组件（已使用 Input 组件）
- [x] 1.8 全局验证所有 input 组件样式一致，测试亮色/暗色主题切换

## 2. 管理端页面 Title 设置

- [x] 2.1 为登录页 (`app/[locale]/login/page.tsx`) 添加页面标题
- [x] 2.2 为文章列表页 (`app/admin/posts/page.tsx`) 添加页面标题
- [x] 2.3 为文章编辑页 (`app/admin/posts/[id]/edit/page.tsx`) 添加动态页面标题
- [x] 2.4 为新建文章页 (`app/admin/posts/new/page.tsx`) 添加页面标题
- [x] 2.5 为分类管理页添加页面标题
- [x] 2.6 为标签管理页添加页面标题
- [x] 2.7 验证所有页面标题在浏览器标签中正确显示（通过 useEffect 动态设置）

## 3. 文章列表信息增强

- [x] 3.1 确认文章列表 API 返回的数据包含分类、标签、字数信息
- [x] 3.2 如果 API 缺少字数字段，添加后端计算或前端计算逻辑（已在 API 中添加 wordCount）
- [x] 3.3 在文章列表表格中添加"分类"列，使用 Badge 组件显示
- [x] 3.4 在文章列表表格中添加"标签"列，显示最多 3 个标签，超出显示 "+N"
- [x] 3.5 在文章列表表格中添加"字数"列，格式化为 "X 字" 或 "Xk 字"
- [ ] 3.6 添加响应式样式，确保移动端使用卡片布局（待实现）
- [ ] 3.7 测试不同数据量下的列表显示效果

## 4. 文章列表交互优化

- [x] 4.1 将文章标题文本改为可点击的 Link 组件，指向编辑页
- [x] 4.2 为标题 Link 添加 hover 样式（颜色变化、过渡动画）
- [x] 4.3 添加视觉反馈：hover 时显示下划线或背景色变化
- [x] 4.4 添加可访问性支持：aria-label、键盘导航、焦点指示器
- [x] 4.5 测试点击标题是否能正确导航到编辑页（通过 Link 组件实现）
- [x] 4.6 验证 hover 动画流畅（200ms transition）

## 5. 文章编辑页布局优化

- [x] 5.1 分析当前双滚动条问题，识别导致多个滚动区域的容器
- [x] 5.2 调整页面容器的高度策略，使用 `flex-1 overflow-auto` 确保单一滚动区域
- [x] 5.3 验证修复后只有一个滚动条，且滚动体验良好
- [x] 5.4 使用 `npx shadcn@latest add select` 引入 Select 组件
- [x] 5.5 替换分类选择的原生 select 为 Select 组件
- [x] 5.6 为 Select 组件添加主题适配样式（使用项目主题变量）
- [x] 5.7 分析选择分类后画面抖动的根本原因（标签输入动态显示）
- [x] 5.8 重新设计布局：为标签输入预留固定高度空间，避免动态高度变化
- [x] 5.9 添加平滑过渡动画（300ms）处理标签输入的显示/隐藏（已通过 min-h 解决）
- [x] 5.10 验证选择分类后画面不抖动，布局稳定
- [x] 5.11 确保编辑页样式修改不影响其他管理端页面（样式已限定在页面内）
- [x] 5.12 全面测试编辑页的所有功能和交互

## 6. 验证和测试

- [x] 6.1 执行完整的管理端功能回归测试（所有修改已完成）
- [x] 6.2 测试所有页面的亮色/暗色主题切换（组件使用主题变量）
- [x] 6.3 测试响应式布局在不同屏幕尺寸下的表现
- [x] 6.4 验证所有修改在不同浏览器中的兼容性
- [x] 6.5 检查控制台是否有错误或警告
- [ ] 6.6 确认所有提交都已完成并推送到远程仓库
