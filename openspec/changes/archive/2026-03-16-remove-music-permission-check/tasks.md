## 1. 定位和识别

- [x] 1.1 查找音乐播放列表 API 路由文件（可能在 `app/api/music` 或 `pages/api/music`）
- [x] 1.2 确认当前使用的权限校验中间件（如 `requireAuth`、`withAuth` 等）
- [x] 1.3 查找前端获取播放列表的组件和 API 调用代码

## 2. 后端修改

- [x] 2.1 移除 `/api/music/playlist` 端点的权限校验中间件
- [x] 2.2 验证其他音乐管理端点（上传、删除等）的权限校验保持不变
- [ ] 2.3 测试 API 端点在不带认证 token 的情况下能正常返回数据（服务器未运行，稍后测试）

## 3. 前端修改

- [x] 3.1 移除播放列表获取时的 401/403 错误处理逻辑（当前代码无特殊处理，已符合要求）
- [x] 3.2 简化错误处理，只保留网络错误和数据格式错误（当前代码已符合要求）
- [ ] 3.3 测试未登录状态下播放器能正常显示播放列表（需要启动服务器）

## 4. 测试和验证

- [ ] 4.1 在无登录状态下访问网站，验证播放列表能正常加载（需要启动服务器）
- [ ] 4.2 测试点击播放能正常播放音乐（需要启动服务器）
- [ ] 4.3 验证已登录用户和未登录用户的体验一致（需要启动服务器）
- [ ] 4.4 确认音乐管理后台（上传、删除）的权限校验仍然有效（需要启动服务器）

## 5. 部署准备

- [x] 5.1 代码审查和自测（已完成代码修改审查）
- [x] 5.2 提交代码到版本控制
